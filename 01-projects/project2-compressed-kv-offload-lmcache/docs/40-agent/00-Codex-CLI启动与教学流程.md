# P2 Codex Startup and Teaching

当前P2 blocked。Session首先验证P1 unlock；未解锁只输出blocked reason与docs review，不probe runtime。

解锁后的启动输出：Parent/Slice/Mode、both repo identities、ABI/env、Approved Designs、Learning Goal、anchors、allowed/read-only/out、evidence/Gate。

教学顺序：

~~~text
connector/object flow
→ tiny MemoryObj/layout/byte hand calculation
→ user prediction
→ pure oracle
→ raw FS baseline
→ approved Serde seam
→ E2E
→ kernel/profile
~~~

Web+用户批准architecture/format/placement；Codex执行最小repository Slice。

