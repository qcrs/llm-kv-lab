# Technical Baseline Snapshots

本目录保存项目进入实现前的完整技术 reasoning snapshot。它们用于防止长期协作时，设计被摘要压缩后重新发明。

## Authority

- 这些文件是 **READ_ONLY_REFERENCE_SNAPSHOT**。
- 它们不是 `PROJECT_STATE.md`，也不自动批准 implementation API / field / patch。
- 若 snapshot 内部有历史结论冲突，以同级 `BASELINE-RESOLUTION.md` 的 canonical resolution 为准。
- 若 snapshot 与 fixed local source/raw evidence 冲突，以 fixed source/raw evidence 为准，并触发 `DESIGN_CONFLICT` 或 TO_VERIFY。

当前 snapshot：`P1-v3-Implementation-Audited-BASELINE.md`。
