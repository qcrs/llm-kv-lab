# P1 V1/V2 Real-Engine E2E Closure

## Source Identity

- workspace: `/home/qcrs/learning/llm-kv-lab`
- worktree: `/home/qcrs/learning/llm-kv-lab/worktrees/p1-vllm-reclaim`
- branch: `p1/v2-token-compaction-v026`
- HEAD: `1f04cdca301fa72d20046a791672cf265bf9df09`
- baseline: `vLLM v0.26.0`

## Runtime

- GPU: `CUDA_VISIBLE_DEVICES=1`, NVIDIA A100 80GB PCIe
- model: `/data/models/Qwen3-0.6B`
- dtype: `bfloat16`
- `block_size=16`, `max_model_len=128`, `max_num_seqs=1`, `max_num_batched_tokens=32`
- `enforce_eager=True`，CUDA Graph off
- `async_scheduling=False`
- `pipeline_parallel_size=1`, `tensor_parallel_size=1`, `data_parallel_size=1`, `decode_context_parallel_size=1`
- `max_concurrent_batches=1`
- KV Connector: off (`connector=None`)
- prefix caching: off；speculative decoding: off
- scheduler: `Scheduler`
- V2 model runner: enabled (`use_v2_model_runner=True`)

## V1 Real Engine

结果：`PASS`。

原始证据：`raw/p1-v2-e2e-closure/v1-inproc.log`，标记 `P1_M1_T3_INTEGRATION_CLOSURE_PASS`。

- OBSERVED：canonical ownership `[1,2,3,4,5,6] -> [1,2,5,6]`。
- OBSERVED：`effective_kv_len` 提交为 `63`；worker transition 的目标为 `62`，包含本步真实 decode token。
- OBSERVED：removed block IDs 为 `[3,4]`，两者 `ref_cnt=0`。
- OBSERVED：free block count `10860 -> 10862`，增量为 `2`。
- OBSERVED：后续 `reuse-b` 实际分配并复用 block `4`，请求完成 `2` 个 output tokens。
- OBSERVED：`reclaim-a` 真实完成 `8` 个 output tokens，无 engine crash。

## V2 Step N Compaction

结果：`PASS`。

原始证据：`raw/p1-v2-e2e-closure/v2-gpu1-pass-attempt-20260913.log`，标记 `P1_V2_E2E_PASS`。

- OBSERVED：`source_E=33`，`keep_member_indices=[0,8,16,24,31]`，因此 `K=5`。
- OBSERVED：旧 geometry 为 `3` blocks，新 geometry 为 `1` block。
- OBSERVED：计划注入前 row 为 `[1,2]`；在 compaction step 调度时扩展为 `[1,2,3]` 以承载 source extent；提交后 canonical row 为 `[1]`。
- OBSERVED：`effective_kv_len` 提交为 `5`。
- OBSERVED：compaction schedule 为 source extent 临时分配第 `3` 个 block，free count 为 `10865 -> 10864`；result commit 释放 `2` 个 trailing blocks，恢复为 `10866`。因此释放数量为 `2`，相对计划注入前净恢复 `+1`。
- OBSERVED：真实 worker 执行 gather、scratch、dense-prefix writeback，并返回 `CompactionResultData`；scheduler 完成 validation、ownership truncate 和 free。

## V2 Step N+1 Append / Read

结果：`PASS`。

- OBSERVED：compaction 后下一次 scheduler event 的 canonical row 仍为 `[1]`，`effective_kv_len=5`。
- OBSERVED：下一步继续执行真实 model step，`num_computed_tokens=34`，并最终完成 request。
- OBSERVED：没有 stale `BlockTable`、slot mapping 或 effective-E assertion；`outputs_seen=2`，request `finished=true`。
- 该 smoke 证明下一步 physical allocation frontier 使用 compacted `K=5`，而不是旧 physical extent；logical token accounting 仍由 request 的原有 `num_computed_tokens` 继续推进。

## Allocator Reuse

结果：`PASS`。

V1 已观察到 removed block `4` 被后续 request 实际重新分配；V2 已观察到 removed blocks 进入 free lifecycle 并恢复 allocator capacity。

## Regression / Verification

- `python -m py_compile 04-experiments/project1_kv_reclaim/scripts/p1_v2_e2e_smoke.py`：`PASS`
- `git -C worktrees/p1-vllm-reclaim diff --check`：`PASS`
- focused regression：`27 passed, 15 skipped`（`tests/v1/core/test_compaction_reconciliation.py`、`tests/v1/worker/test_gpu_compaction_preparation.py`、`tests/v1/worker/test_gpu_effective_kv_state.py`）。
- 未运行全仓 pytest；real-engine smoke 日志已保存上述 raw evidence。

## 限制与边界

本轮只证明 explicit synchronous、single-GPU、single-KV-group、eager execution regime 下的 V1/V2 functional core。未证明或未支持：async scheduling、`PP>1`、KV Connector、prefix caching、speculative decoding、CUDA Graph、正式 benchmark 及性能结论。

## 结论

- V1 Functional Core：`CLOSED`
- V2 Functional Core：`CLOSED`
- T4 Release / Reuse：`CLOSED`
- Real-Engine E2E：`PASS`
- DESIGN_CONFLICT：`NONE`

## Evidence Index

- `raw/p1-v2-e2e-closure/v1-inproc.log`
- `raw/p1-v2-e2e-closure/v2-gpu1-pass-attempt-20260913.log`
- `scripts/p1_v2_e2e_smoke.py`
